SRC_RD_TOKEN=your_trondheim_token
DST_RD_TOKEN=your_stavanger_token

# start safe
MIRROR_MODE=add-only
DRY_RUN=true

SYNC_INTERVAL=45s
RUN_TIMEOUT=10m
HTTP_TIMEOUT=20s
WRITE_DELAY=250ms

MAX_RETRIES=4
RETRY_BASE=500ms
RETRY_MAX_JITTER=350ms
PAGE_LIMIT=250

# Optional: do not delete destination torrents matching this regex in mirror-delete mode.
# PROTECT_DST_REGEX=^\[KEEP\]

# Optional status endpoints
HEALTH_ADDR=:8099

# Optional API override for tests/self-hosted mirrors
# RD_API_BASE_URL=https://api.real-debrid.com/rest/1.0
